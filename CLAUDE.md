# City Builder — Claude Instructions

## After Every Change

After completing any code change, always commit and push to GitHub:

```bash
cd "C:/Users/dbeve/source/repos/city-builder"
git add -A
git commit -m "<concise description of what changed>"
git push
```

Use a descriptive commit message that summarises what was changed and why.
